import { Link } from 'react-router-dom';
import '../styles/Header_centered.css';

export default function Header() {
    return (
        <header className="header">
        <h1 className="title">OURFRIDGE</h1>
      </header>
      );
}